{
    "buildCommand": "node scripts/inject-env.js && npm run build",
    "outputDirectory": "dist",
    "framework": "angular",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}